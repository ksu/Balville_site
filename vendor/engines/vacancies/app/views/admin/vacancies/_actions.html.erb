<ul>
  <% if Admin::VacanciesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_vacancies_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_vacancy_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::VacanciesController.sortable? and Vacancy.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Vacancies"),
                 admin_vacancies_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Vacancies"),
                 admin_vacancies_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>